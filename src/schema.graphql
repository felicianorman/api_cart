type Cart {
  id: ID
  totalAmount: String
  products: [Product]
}

type Product {
  productId: ID
  name: String
  price: Int
}

type Query {
  getCartById(cartId: ID!): Cart
}

type Mutation {
  createCart: Cart
  createProduct(input: CreateProductInput): Product 
}

input CreateProductInput {
  name: String!
  price: Int!
}